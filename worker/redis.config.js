const config = {
    port: 6388, // Redis port
    host: "47.106.21.200", // Redis host
    // family: 4, // 4 (IPv4) or 6 (IPv6)
    password: "",
    connectTimeout: 10000,
    db: 0,
}
module.exports = config;
